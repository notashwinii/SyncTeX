version: "2"

sql:
  - engine: postgresql
    schema: migrations
    queries: internal/db/query
    gen:
      go:
        package: db
        out: internal/db
        sql_package: pgx/v5
        emit_interface: true
